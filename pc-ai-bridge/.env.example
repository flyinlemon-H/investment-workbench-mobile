# Copy to .env for local use. Never commit the real .env file.
DEEPSEEK_API_KEY=
PC_AI_BRIDGE_PORT=18765
PC_AI_BRIDGE_ALLOWED_ORIGINS=https://flyinlemon-h.github.io
# Set to mock only for deterministic, non-paid local acceptance tests.
PC_AI_BRIDGE_MODE=deepseek
